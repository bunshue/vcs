### 21.2.2 ###

當作破折號的雙連字符號、用於換行的連字符號、以及
其他任何標點符號都可能是潛在的問題。加強第18章中
所建立的單字清理模組將是解決大多數問題的好方法。

### 21.2.3 ###

你可以使用 list 生成式來轉換這些欄位

### 21.2.4 ###

在不使用csv模組的情況下，你必須檢查欄位是否以引號
字元開始和結束，然後用strip()將它們剝離。 

要在不使用csv函式庫的情況下處理內嵌的分隔符號，
你必須隔離有引號的欄位，並以不同方式對待它們，
然後才能使用分隔符號拆分其餘的欄位。

### 21.4.1 ###

clean_field = [float(x[13]) for x in data_rows if x[13] != 'Missing']

---

average = sum(clean_field)/len(clean_field)





