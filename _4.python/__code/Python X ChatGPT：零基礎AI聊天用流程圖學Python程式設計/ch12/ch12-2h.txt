我寫了一個BMI計算程式，可以執行結果並不正確，請幫我更正程式碼的錯誤，和使用程式框來顯示程式碼，如下：
height = float(input("請輸入您的身高（公分）：")) / 100
weight = float(input("請輸入您的體重（公斤）："))

bmi = weight / height * height

print("您的BMI值為：", round(bmi, 2))
