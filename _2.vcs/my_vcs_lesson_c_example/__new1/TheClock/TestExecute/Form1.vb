Imports Microsoft.VisualBasic
Public Class Form1

    Private Sub Form1_Load(ByVal sender As Object, ByVal e As System.EventArgs) Handles Me.Load
        'Me.Text = Format(Now + TimeSpan.FromHours(2), "hh:mm:ss").ToString
        'Me.Text = TimeZone.CurrentTimeZone.GetUtcOffset(Now).ToString

        'Dim frm As New TheClock.TimezoneForm
        'frm.Show()
    End Sub
End Class
